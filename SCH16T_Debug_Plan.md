# SCH16T 传感器解锁重启问题 - 调试计划

## 问题现状
- **现象**: 解锁时传感器重启
- **示波器**: GPIO_EMC_B2_09 (EXTRESN 复位引脚) 在解锁时跳动
- **已尝试修复**: 禁用硬件复位功能，但问题依然存在

## 疑问点

### 1. 示波器观察的时间点
- **是否**在最新固件上测试的？（已禁用 SPI3_RESET）
- 跳动是在解锁**瞬间**还是解锁**之后持续**？
- 跳动频率是多少？

### 2. 可能的干扰源

#### A. GPIO 初始化配置
```c
GPIO_SPI3_nRESET_SENSOR3  // 配置为输出，初始HIGH
```
- 这个引脚在 PX4_GPIO_INIT_LIST 中
- 启动时会被初始化为 HIGH（不复位状态）
- **可能问题**: 解锁时重新配置？

#### B. 引脚复用冲突
GPIO_EMC_B2_09 可能的复用功能：
- GPIO2_IO19（当前用途）
- FLEXPWM?（需要查硬件手册）
- 其他外设？

#### C. 电源噪声
- 解锁时PWM启动 → 电流冲击
- GPIO1_IO14（SCH16T电源）可能受干扰
- GPIO2_IO19 (复位引脚) 也可能受干扰

### 3. board_spi_reset() 的影响
rc.board_sensors 中调用：
```bash
board_spi_reset 100 4  # 复位SPI3总线
```

这个函数会：
1. 关闭 GPIO1_IO14 (电源)
2. 等待 100ms
3. 打开 GPIO1_IO14 (电源)
4. 重新配置 CS 和 DRDY 引脚

**但不操作 GPIO_SPI3_nRESET_SENSOR3！**

## 下一步测试

### 测试 1: 确认最新固件
```bash
# 上传最新固件后
dmesg | head -20  # 检查编译时间
dmesg | grep -i sch16t
```

### 测试 2: 禁用 GPIO 初始化
临时从 PX4_GPIO_INIT_LIST 移除 GPIO_SPI3_nRESET_SENSOR3，看引脚是否还跳动

### 测试 3: 监控引脚状态
在NSH中运行（如果命令存在）：
```bash
gpio read GPIO2_IO19  # 解锁前
commander arm
gpio read GPIO2_IO19  # 解锁后
```

### 测试 4: 简化测试
1. 不启动SCH16T驱动
2. 只解锁飞机
3. 观察示波器

```bash
# NSH中
sch16t stop  # 停止驱动
commander arm  # 解锁
# 观察示波器
```

## 可能的根本原因

### 假设 A: 传感器本身的问题
- 解锁后电源噪声导致传感器异常
- 传感器自身拉低 EXTRESN 引脚
- **解决方案**: 硬件滤波、隔离电源

### 假设 B: 引脚复用冲突
- 解锁时其他外设激活并使用了这个引脚
- **解决方案**: 检查 IOMUX 配置

### 假设 C: 驱动逻辑问题
- 虽然禁用了宏，但有其他路径操作引脚
- **解决方案**: 全局搜索所有对 GPIO2_IO19 的操作

## 建议

**如果引脚确实在跳动，而代码无法操作它，最可能的原因是：**
1. **硬件问题**: 传感器自己在拉低复位引脚（因为电源不稳定）
2. **引脚复用**: 某个外设在解锁时使用了这个引脚

**建议测试：**
1. 物理断开 EXTRESN 引脚连接
2. 固定 EXTRESN 为 HIGH（3.3V）
3. 观察传感器是否还重启

如果传感器还重启 → 是SPI通信或电源问题
如果传感器稳定 → 确实是复位引脚被操作
